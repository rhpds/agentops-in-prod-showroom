= モジュール 3: エージェント型アプリケーションのメトリクスとログ
:imagesdir: ../assets/images
:navtitle: モジュール 3: メトリクスとログ
:toc: macro
:toclevels: 1

メトリクスは、システムで**何が**起きているかを教えてくれます。ログは、**正確に何が、いつ起きたか**を教えてくれます。このモジュールでは、mortgage-ai マルチエージェントシステムを監視する事前にデプロイされた Grafana ダッシュボードを探索し、次に OpenShift Console の LokiStack を使用してアプリケーションのログを調査します。

TIP: このモジュールでは、あなたはシステムの健全性、リクエスト率、エラー率、および運用ログに焦点を当てる **SRE / プラットフォームエンジニア** の役割を担います。

== 学習目標

このモジュールを修了すると、以下のことができるようになります：

* マルチエージェント AI システムにとって重要な主要メトリクスを特定できる
* 事前にデプロイされた Mortgage-AI Grafana ダッシュボードを操作できる
* LokiStack と LogQL (Log Query Language) を使用してアプリケーションログを探索およびフィルタリングできる
* エージェント型のオブザーバビリティにおいて、メトリクスとログがどのように連携するかを説明できる

== エージェント型のメトリクスは何が違うのか？

従来のアプリケーションでは、リクエスト率、エラー率、レイテンシが必要です。エージェント型のアプリケーションでは、それらすべてに**加えて**、LLMの動作、エージェントのルーティング、およびツールの実行をキャプチャするメトリクスが必要です。以下は、最も重要な6つのメトリクスです：

[cols="2,2,3",options="header"]
|===
|メトリクス |Prometheus での名前 |わかること

|リクエスト率とエラー
|`http_requests_total`
|どのくらいのトラフィックが来ているか？ いくつのリクエストが失敗しているか？

|レイテンシのパーセンタイル
|`http_request_duration_seconds`
|どのくらい速く応答しているか？ 外れ値はあるか？

|LLM トークン使用量
|`llm_tokens_total`
|いくつのトークンを消費しているか？ 1回の会話あたりのコストはいくらか？

|LLM 推論のレイテンシ
|`llm_inference_duration_seconds`
|モデルの応答にどれくらいの時間がかかるか？ それがボトルネックになっているか？

|ツール呼び出しの成功率
|`tool_calls_total`
|エージェントのツールは機能しているか？ どのツールが失敗しているか？

|エージェントのルーティング
|`agent_routing_total`
|クエリは高速なモデルにルーティングされているか、それとも高性能なモデルにルーティングされているか？ 分類器は機能しているか？
|===

image::agentic-app-pre4.png[Agentic Metrics - The 6 That Matter showing traditional metrics plus agentic additions with model routing and Prometheus scraping by OpenShift UWM,link=self,window=blank,width=700]

mortgage-ai の API は、これらすべてを `GET /metrics` で Prometheus 形式にて公開しています。**OpenShift User Workload Monitoring**（Red Hat OpenShift AI に付属する組み込みの Prometheus インスタンス）は、30秒ごとに自動的にそれらをスクレイピングし、時系列データベースに保存します。

つまり、Prometheus を自分でセットアップする必要はありません。プラットフォームが収集と保存を処理します。あなたが行う必要があるのは以下のことだけです：

1. アプリケーションからメトリクスを公開する（mortgage-ai はすでにこれを行っています）
2. 視覚化プラットフォーム（このワークショップでは Grafana を使用）を設定して、OpenShift の Prometheus データソース（設定済み）にクエリを実行する
3. メトリクスを視覚化するためのダッシュボードを構築する（これについては次で探索します）

[%collapsible]
.完全なメトリクスリファレンス (クリックして展開)
====
mortgage-ai のバックエンドは、`packages/api/src/core/metrics.py` で定義された 10 個のカスタムメトリクスを公開しています：

[cols="2,1,2",options="header"]
|===
|Prometheus での名前 |タイプ |ラベル

|`llm_tokens_total`
|カウンター
|`model`, `direction` (入力/出力), `persona`

|`llm_inference_duration_seconds`
|ヒストグラム
|`model`, `persona`

|`agent_routing_total`
|カウンター
|`persona`, `complexity` (単純/複雑)

|`agent_escalation_total`
|カウンター
|`persona`, `reason`

|`tool_calls_total`
|カウンター
|`tool_name`, `persona`, `status` (成功/エラー)

|`tool_call_duration_seconds`
|ヒストグラム
|`tool_name`, `persona`

|`active_chat_sessions`
|ゲージ
|`persona`

|`chat_messages_total`
|カウンター
|`persona`, `direction` (受信/送信)

|`loan_applications_total`
|カウンター
|`status`, `loan_type` (まだ計装されていません)

|`compliance_checks_total`
|カウンター
|`check_type`, `result` (まだ計装されていません)
|===

さらに、`prometheus-fastapi-instrumentator` は自動の HTTP メトリクス (`http_requests_total`, `http_request_duration_seconds_bucket`) を提供します。
====

== 演習 1: Mortgage-AI Grafana ダッシュボードの探索

ダッシュボードにアクセスするには、**Grafana** タブに移動します。

image::grafana-tab.png[]

プロンプトが表示されたら、資格情報を使用して再度ログインします：

* **Username**: {user}
* **Password**: {password}

この時、"Authorize Access"という画面が表示されることがありますが、必要なアクセス権の付与のため、画面下部のボタンの左側、"Allow selected permissions"をクリックして下さい。

Grafana インスタンスは、ワークスペースのネームスペースに事前デプロイされており、**Mortgage-AI Backend Metrics** ダッシュボードがすでに設定されています。これは 2 つの Prometheus データソースに接続します：

* **User Workload Monitoring**: mortgage-ai アプリケーションのメトリクスをスクレイピングします (デフォルト)
* **Red Hat OpenShift AI (RHOAI) Prometheus**: OpenShift AI からのモデルサービングメトリクスを提供します

開いて、何があるか見てみましょう。

. **Grafana** タブを開きます。

. **Mortgage-AI Backend Metrics** ダッシュボードに移動します。

image::open-grafana-sidebar.png[]
image::open-metrics-dashboard.png[]

=== 概要(Overview)と HTTP パネル

ダッシュボードの上部では、システムの健全性を一目で確認できます：

image::grafana2.png[Grafana Overview and HTTP panels showing request rate and latency,link=self,window=blank,width=700]

表示されている内容は以下の通りです：

* **Total Requests (Last Hour)**: API にヒットしたリクエスト数。スクリーンショットでは約 983 リクエスト。
* **Request Rate**: 現在の1秒あたりのリクエスト数（スループット）。
* **Error Rate (%)**: 5xx 応答の割合。緑色は健全であることを意味します。
* **P95 Latency**: 95% のリクエストがこの値よりも速く完了します。100ms 以下であれば素晴らしいです。

概要の下にある HTTP パネルでは、トラフィックをエンドポイントとステータスコード別に分類しています。以下に注目してください：

* 最もトラフィックが多いエンドポイントはどれか (おそらく `/api/prospect/chat`)
* HTTP Status Distribution に黄色 (4xx) または赤色 (5xx) のバーがあるか
* 最も P95 レイテンシが高いエンドポイントはどれか

[%collapsible]
.Overview パネルの PromQL (Prometheus Query Language) の詳細 (クリックして展開)
====
* **Request Rate**: `sum(rate(http_requests_total{namespace=~"$namespace"}[5m]))`
* **Error Rate**: `(sum(rate(http_requests_total{namespace=~"$namespace", status=~"5.."}[5m])) / sum(rate(http_requests_total{namespace=~"$namespace"}[5m])) * 100) or vector(0)`
* **P95 Latency**: `histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{namespace=~"$namespace"}[5m])) by (le))`

`rate()` は、特定の時間枠における1秒あたりの平均値を計算します。`histogram_quantile()` は、ヒストグラムのバケットからパーセンタイルを計算します。
====

=== AI エージェントメトリクス

下にスクロールして **AI Agent Metrics** の行を表示します。ここからが面白くなります。これらのメトリクスは、エージェント型のアプリケーションに固有のものです：

image::grafana1.png[Grafana AI Agent Metrics showing token usage and tool call success rate,link=self,window=blank,width=700]

このセクションの上部にある 4 つの統計パネルには、以下が表示されます：

* **Active Chat Sessions**: ライブの WebSocket 接続数。現在何人のユーザーがチャットしているか？
* **Total LLM Tokens**: API が開始されてから消費されたトークンの累積数。これはコストの指標となります。スクリーンショットでは 18.5K トークンです。
* **Tool Call Success Rate**: エージェントのツールは機能しているか？ 100% はすべてが健全であることを意味します。
* **Avg LLM Latency**: LLM 推論呼び出しの中央値（レイテンシ）。スクリーンショットでは 1.75秒です。

統計の下にある詳細パネルには、以下が表示されます：

* **LLM Token Usage by Model**: 最もトークンを消費しているモデルはどれか？ 入力と出力のどちらが支配的か？
* **LLM Inference Latency by Persona**: 最も遅いエージェントのペルソナはどれか？ アンダーライターエージェントが遅い場合、複雑なコンプライアンスツールの呼び出しを示している可能性があります。
* **Agent Routing Distribution**: 単純なルーティングと複雑なルーティングの円グラフ。複雑なクエリの比率が高い場合、高性能なモデルが作業の大部分を行っていることを意味します。
* **Tool Calls by Status**: 成功は緑、失敗は赤。赤のスパイクは、ツールの統合が壊れていることを意味します。

[%collapsible]
.AI Agent Metrics の PromQL の詳細 (クリックして展開)
====
* **LLM Tokens**: `sum(increase(llm_tokens_total{namespace=~"$namespace"}[1h]))`
* **Tool Success Rate**: `(sum(tool_calls_total{namespace=~"$namespace", status="success"}) / sum(tool_calls_total{namespace=~"$namespace"}) * 100) or vector(100)`
* **LLM Latency**: `histogram_quantile(0.50, sum(rate(llm_inference_duration_seconds_bucket{namespace=~"$namespace"}[5m])) by (le))`
* **Routing Distribution**: `sum(agent_routing_total{namespace=~"$namespace"}) by (complexity)`
====

TIP: このダッシュボードは `GrafanaDashboard` カスタムリソースとして管理され、GitOps を通じてバージョン管理されています。これを確認するには、`oc get grafanadashboard -n {user_project}` を実行してください。

モジュール 1 で生成したトラフィック (製品の探索、CEOのポートフォリオの健全性) は、すでにこれらのパネルに反映されています。時間範囲を **Last 1 hour** (過去1時間) に設定して確認してください。

== 演習 2: LokiStack によるアプリケーションログの探索

メトリクスは集約された健全性を示しますが、問題が発生した場合は実際のログイベントが必要です。Red Hat AI には、アプリケーションが STDOUT または STDERR に書き込んだすべてを自動的に収集する高性能なログ集約システムである **LokiStack** が含まれています。コードの変更は必要ありません。mortgage-ai のログを探索してみましょう。

=== ログビューアへの移動

. **OCP Console** タブに移動します。次に **モニタリング(もしくはObserve)** → **Logs** に移動します。
+
image::observe-logs.png[]
+
**Forbidden — Missing permissions to get logs (禁止されています — ログを取得する権限がありません)** というエラーが表示されます：
+
image::logging4.png[OpenShift Console Observe Logs showing Forbidden error for regular user,link=self,window=blank,width=700]
+
これは想定通りの動作です。あなたは一般ユーザー (`{user}`) としてログインしているため、セキュリティ上の理由から、OpenShift はログへのアクセスをあなたのネームスペースのみに制限します。クラスター全体のログへのアクセスは、クラスター管理者に予約されています。

=== ログをネームスペースにスコープする

[start=2]
. **Show Query** をクリックしてクエリエディタを開きます：
+
image::logging5.png[OpenShift Console Logs Show Query button,link=self,window=blank,width=700]
+
この LogQL クエリを入力して、ログのスコープをあなたのネームスペースに絞り込みます：
+
[source,logql,role=execute,subs=attributes+]
----
{ log_type="application", kubernetes_namespace_name="{user_project}" }
----
+
**Run Query** をクリックします。これで、mortgage-ai ポッドからのログが表示されるはずです。
+
TIP: `log_type="application"` は、インフラストラクチャログ (kubelet, CRI-O) を除外します。`kubernetes_namespace_name` ラベルは、ログコレクターによって自動的に挿入されます。

=== ログフォーマットの理解

表示されているのは構造化された JSON です。各ログエントリには以下が含まれています：

* **Kubernetes メタデータ**: ポッドID、コンテナ名、ネームスペースラベル、ネットワーク設定
* **アプリケーションメッセージ**: アプリケーションからの実際のログイベント (`message` フィールド内)
* **タイムスタンプ**: イベントが発生した日時

ログエントリをスクロールして `message` フィールドを見つけます。そこに有用なアプリケーションデータが存在します。メタデータは、コンテナ間でログを関連付けたり、ポッド、ネームスペース、またはコンテナでフィルタリングしたりするのに役立ちます。

TIP: ログエントリ内の `message` フィールドをすばやく見つけるには、ブラウザの検索機能（Macの場合は **Cmd+F**、Windows/Linuxの場合は **Ctrl+F**）を使用して `"message"` を検索します。これにより、Kubernetes のメタデータをスキップして、アプリケーションのログコンテンツに直接ジャンプできます。

=== アプリケーションメッセージの抽出

[start=3]
. LogQL を使用してアプリケーションメッセージのみを抽出し、API コンテナのログにフィルタリングします：
+
[source,logql,role=execute,subs=attributes+]
----
{ log_type="application", kubernetes_namespace_name="{user_project}", kubernetes_container_name="api" }
| json
| line_format "{{.message}}"
----
+
これにより、Kubernetes のメタデータが取り除かれ、アプリケーションがログに書き込んだ内容のみが表示されます。

=== ログ分析において AI がどのように役立つか

生のログには豊富なデータが含まれていますが、JSON を手動で解析するのは面倒です。本番環境では、チームは AI を搭載したオブザーバビリティツールを使用して洞察を抽出できます：

**AIアシスタントがこれらのログでできること:**

* 「過去1時間の CEO エージェントからのすべてのエラーを表示して」
* 「午後2時のレイテンシの急増の原因は何でしたか？」
* 「今日の失敗したツール呼び出しを要約して」

**AI が生成した洞察の例:**

[source,text]
----
mortgage-ai ログの分析 (過去1時間):
• 42回のツール呼び出し: 40回成功、2回失敗
• 失敗: get_credit_score タイムアウト (30秒)
• 最も遅いツール: get_pipeline_summary (平均 2.3秒)
• 最もアクティブなエージェント: Prospect (27リクエスト)
----

生データは LokiStack に存在します。AI は、手動での JSON 解析や複雑なクエリなしに、そのデータへのアクセスを可能にします。

=== ログとトレースは補完し合う関係にある

ログは、エラー、警告、デバッグ出力、ツールの結果など、詳細なイベントをキャプチャします。これらは以下に不可欠です：

* 特定の障害のデバッグ (「エラーメッセージは何だったか？」)
* コンプライアンスイベントの監査 (「誰がこの PII にアクセスしたか？」)
* 予期しない動作の調査 (「なぜこの計算がゼロを返したのか？」)

**モジュール 4 ではトレースを紹介します。** トレースは以下の機能を提供することでログを補完します：

* エージェントとツールにまたがる階層的なリクエストフロー
* 各ステップの所要時間の内訳
* 何が何を呼び出したかを示す親子関係

ログとトレースを組み合わせることで、完全な可視性が得られます。詳細なイベントにはログを、リクエストフローにはトレースを使用します。

IMPORTANT: **LokiStack は Red Hat AI のサポート対象機能です。** ログはポッドの寿命を超えて保持されます。コンテナが再起動または再デプロイされた場合でも、LokiStack は履歴ログを保持するため、インシデント発生後の調査が可能になります。マルチエージェントシステムの場合、API、UI、およびデータベースコンテナ間のイベントを関連付けるために、この永続的なログストレージが不可欠です。

== モジュールのまとめ

**達成したこと:**

* HTTP と AI 固有のエージェントメトリクスを備えた Grafana ダッシュボードを探索しました
* LokiStack と LogQL クエリを使用してアプリケーションログをフィルタリングしました
* API コンテナと UI コンテナ間でログエントリを関連付けました

**重要なポイント:**

* エージェント型のアプリには、HTTP 以外にもトークン使用量、推論のレイテンシ、ルーティングの決定、ツールの成功率などのメトリクスが必要です
* ログはメトリクスでは集約されて見えなくなる詳細を提供します。この詳細は、エフェメラル(短命)なコンテナのデバッグに不可欠です。
* メトリクスは「どれくらい？」「どれくらい速い？」に答えます。ログは「正確に何が起きたか？」に答えます。

**次のステップ:**

モジュール 4 では MLflow のトレースについて紹介します。これは、**何が**起きているかだけでなく、**なぜ**それが起きるのかを理解するための鍵となります。
