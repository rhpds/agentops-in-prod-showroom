= 模块 1：智能体应用与可观测性的必要性
:imagesdir: ../assets/images
:navtitle: Module 1: The Agentic App
:toc: macro
:toclevels: 1

AI 智能体应用不会简单直接报错，故障往往分散在整个分布式链路中。本模块将带你体验已部署完成的抵押贷款多智能体应用，并理解传统监控手段为何无法适配这类复杂 AI 系统。

首先明确：什么是 AI 智能体？AI 智能体依托大语言模型（LLM）完成任务推理、自主选择调用工具并执行自主操作，区别于传统接口、基础聊天机器人这类简单一问一答模式。当多个智能体协同工作，各自配备专属工具、承担独立业务职责时，就构成多智能体系统。这类系统业务能力强大，但内部链路复杂，排障与可视化难度大幅提升。

联邦奥若资本需要多智能体全流程可视化能力，本次由你负责验证可观测体系的落地价值。在搭建解决方案前，你需要先熟悉整套应用架构。

== 学习目标

完成本模块学习后，你将具备以下能力：

* 描述抵押贷款多智能体系统整体架构
* 区分 5 类业务智能体角色，并掌握各自职责
* 说明分布式 AI 系统需要专用可观测方案的原因
* 识别多智能体架构中常见的故障模式（系统异常、性能衰减场景）

== 实操练习 1：访问应用前端界面

TIP: 本工作坊所有核心服务均通过标签页*tabs*打开，无需额外新开浏览器窗口。 

. 切换至**Terminal**标签页，登录 OpenShift 集群： 
+
[source,console,role=execute,subs=attributes+]
----
oc login --insecure-skip-tls-verify $(oc whoami --show-server) -u {user} -p {password}
----

. 切换至*OCP Console*标签页，使用刚才登录的账号密码登录：用户名`{user}`，密码`{password}` : 
+
image::agentic-app-pre1.png[OpenShift SSO login page,link=self,window=blank,width=500]

. 登录成功后进入项目列表页面，仅存在一个项目：`{user_project}`。依次进入**Workloads > Pods**，即可查看命名空间内运行的抵押贷款 AI 服务组件:
+
image::workloads-pods.png[]
+
若未显示 Pod 资源，请先确保选择你的项目:
+
image::select-project.png[]
image::agentic-app0.png[OpenShift Console Pods view showing mortgage-ai components running,link=self,window=blank,width=700]
+
你会在练习2中，浏览这些组件.

. Navigate to the *Mortgage AI App* tab:

+
image::mortgage-app.png[]
+

. 页面向下滚动，点击**浏览产品**按钮发起测试对话:
+
image::explore-products.png[Explore Products button on the Fed Aura Capital landing page,link=self,window=blank,width=400]
+
客户咨询智能体会返回各类贷款产品介绍:
+
image::agentic-app2.png[Mortgage-AI chat interface with Prospect Agent responding with loan options,link=self,window=blank,width=700]

注意事项: 大模型后端存在调用频次限制，偶尔会出现请求超时。若智能体无响应或页面报错，刷新页面后重新提问即可。

注意事项: 本应用基于生成式 AI 开发，输出内容具备不确定性，文字表述、排版、细节信息会与教程截图存在差异。

== 实操练习 2：拆解多智能体底层架构

体验完应用交互后，我们深入了解后台运行逻辑。抵押贷款 AI 系统仅部署一套 API 服务，内部集成 5 套独立智能体，对外支撑五类业务角色。.

=== 架构说明

整套抵押贷款 AI 系统封装为**单个 FastAPI 服务 Pod，内部内置 5 套 LangGraph 智能体逻辑**。每个智能体对应一类业务角色，全部运行在同一个pod内。

**系统架构链路:**

image::agentic-app-pre2.png[Mortgage AI system architecture showing React frontend connecting via WebSocket to FastAPI backend with 5 agent personas and PostgreSQL with pgvector and MinIO object storage,link=self,window=blank,width=700]

**部署组件清单:**

[cols="1,2",options="header"]
|===
|组件 |用途

|mortgage-ai-api
|**单 FastAPI pod** 内置全部 5 套智能体逻辑，通过 WebSocket 根据角色路由请求

|mortgage-ai-ui
|React 前端页面，提供智能体交互界面

|mortgage-ai-db
|PostgreSQL 数据库，搭载 pgvector 插件，用于文档存储、RAG 嵌入式

|minio
|兼容 S3 协议的对象存储，存放用户上传业务文件

|keycloak
|身份认证服务（本工作坊已禁用）
|===

=== 五个智能体角色介绍

每个智能体拥有独立端点、工具与业务职责，智能体之间**不会直接相互调用**——相反，它们是服务于不同用户角色的独立工作流程:

image::agentic-app-pre3.png[The 5 agent personas showing Prospect and Borrower and Loan Officer and Underwriter and CEO with their roles and capabilities routing into a single FastAPI deployment via WebSocket,link=self,window=blank,width=700]

[cols="1,1,2,2",options="header"]
|===
|身份 |角色 |智能体 |核心能力

|潜在客户
|Unauthenticated
|Public Assistant
|贷款产品介绍、可贷额度测算

|借款人
|`borrower`
|Borrower Assistant
|贷款申请录入、材料上传、进度查询、补充材料应答

|信贷专员
|`loan_officer`
|LO Assistant
|业务流程管理、申请单审核、沟通话术生成、知识库检索

|风控审核员
|`underwriter`
|Underwriter Assistant
|风险评估、合规校验、补充材料管理、贷款审批决策

|企业高管
|`ceo`
|CEO Assistant
|业务数据分析、审计追溯、决策链路查询、模型运行监控
|===

**API 端点（全部由 mortgage-ai-api pod承载）:**

[source,text]
----
/api/prospect/chat    - 潜在客户智能体（售前咨询）
/api/borrower/chat    - 借款人智能体（申请录入）
/api/loanofficer/chat - 信贷专员智能体（pipeline管理）
/api/underwriter/chat - 风控审核智能体（风险评估）
/api/ceo/chat         - 高管分析智能体（数据分析）
----

=== 请求完整流转链路

**请求路径:** User → UI → WebSocket → API router → Specific agent → LangGraph workflow → Tools (DB, MCP, LLM) → Response

**示例场景 (高管提问： "What's the portfolio health?"，中文亦可："当前业务资产整体状况如何？"):**

. 用户在前端页面输入问题
. WebSocket 根据登录高管身份，路由请求至/api/ceo/chat接口
. 高管智能体（LangGraph 流程）接收提问
. 智能体自主判定需要调用工具/tools：`get_pipeline_summary`, `calculate_pull_through_rate`(获取业务汇总数据,计算贷款转化率)
. 工具分别查询数据库或调用外部 MCP tools
. 智能体多次调用大模型，结合工具返回数据完成推理分析
. 整合结论后将回答返回前端用户

**可观测性核心痛点:** 用户单次提问，背后可能触发 5 次以上大模型调用、十余次工具调用、多轮数据库查询。一旦结果异常或流程报错，必须完整追溯整条决策链路，才能定位根因。

重点: **多智能体架构常见故障模式** +
理解系统故障形式，是搭建有效可观测体系的前提: +
 +
• **工具静默失效**: MCP 工具返回空数据，但智能体继续基于残缺信息生成回复，无任何报错提示 +
• **延迟连锁叠加**: 数据库查询缓慢拖慢大模型调用，最终整体响应超时，无法定位耗时瓶颈步骤 +
• **上下文溢出**: 智能体向大模型传入数据量超限，调用失败仅返回模糊报错，用户仅看到通用超时提示 +
• **非确定性故障**: 相同提问因大模型输出随机性得到不同结果，问题难以复现 +
• **分布式连锁故障**: 某一个智能体工具调用失败，影响下游智能体流程，且错误无法清晰传递

=== Verify the deployment

Before exploring the application, let's confirm all components are running and healthy. We'll check two things: that the OpenShift pods are ready, and that the API can successfully connect to its database.

. Navigate to the **Terminal** tab. View the pods and their status:
+
[source,bash,role=execute,subs=attributes+]
----
oc get pods -n {user_project}
----
+
This command shows all pods in your namespace and their readiness status. All pods should show `Running` with `1/1` or `2/2` containers ready. If any pod shows `CrashLoopBackOff`, `Error`, or `0/1`, the deployment has not completed successfully.

. Check the API service health endpoint:
+
[source,console,role=execute,subs=attributes+]
----
MORTGAGE_HEALTH=$(oc get route mortgage-ai-api-health-route -n {user_project} -o jsonpath='{.spec.host}')
curl -sk https://$\{MORTGAGE_HEALTH}/health/ | jq -r .
----
+
This health check verifies two critical dependencies for the multi-agent system: the FastAPI service itself, and its connection to the PostgreSQL database (which stores application state, embeddings for RAG, and audit trails).
+
Expected output:
+
[source,json]
----
[
  {
    "name": "API",
    "status": "healthy",
    "message": "API is running",
    "version": "0.1.0",
    "start_time": "2026-04-07T22:21:27.657181+00:00"
  },
  {
    "name": "Database",
    "status": "healthy",
    "message": "PostgreSQL connection successful",
    "version": "0.1.0",
    "start_time": "2026-04-07T22:21:19.877102+00:00"
  }
]
----
+
**What this result means:** Both the API and Database components report `"status": "healthy"`. This confirms the FastAPI pod is running and can establish a connection to PostgreSQL. Without a healthy database connection, the agents cannot access their knowledge base, retrieve application data, or execute RAG workflows. If either component shows a status other than `"healthy"`, troubleshoot that service before proceeding.

== Exercise 3: Experience the observability gap

Now that you understand the 5 agent personas and how they collaborate, let's experience firsthand the observability challenge that comes with distributed AI systems.

You'll interact with the application as a CEO and ask the assistant a question. The response will look correct, but you'll have no way of knowing what happened behind the scenes.

. From the application landing page, click **Sign In**. 

+
image::sign-in.png[]
+

. In the sign-in dialog, use the **Persona Demo Login** section at the bottom and select the **CEO** persona:

+
image::agentic-app4.png[Sign in dialog with Persona Demo Login showing CEO selection,link=self,window=blank,width=700]
+
. Click `Sign In`:

+
image::ceo-sign-in.png[]
+

. After signing in, you'll land on the **Executive Dashboard**, the CEO persona's view of portfolio health and operations:
+
image::agentic-app5.png[CEO Executive Dashboard showing Pipeline Overview and Denial Analysis,link=self,window=blank,width=700]

. In the **Your Assistant** chat panel on the right, type the following question (if needed, adjust the center divider to reveal the chat panel or select the chat icon in the bottom right corner):
+
[source,console,role=execute,subs=attributes+]
----
Show me the current pipeline status
----

. The assistant responds with a detailed portfolio health overview: active applications, stage breakdown, pull-through rate, and average days to close:
+
image::agentic-app6.png[CEO Assistant responding with portfolio health overview,link=self,window=blank,width=700]

=== Build a conversation for later analysis

Before we move on, let's generate a richer session with multiple conversation exchanges (called "turns" in AI terminology — each turn is one question from you and one response from the agent). This will give us more data to explore when we get to tracing in Module 4.

[start=5]
. In the same chat, ask a follow-up question:
+
[source,console,role=execute,subs=attributes+]
----
What are the denial trends?
----

. Then ask one more:
+
[source,console,role=execute,subs=attributes+]
----
How are my loan officers doing?
----

Each question triggers a different tool call behind the scenes. By the time you finish, your CEO session will have 3 turns spanning pipeline health, denial analysis, and officer performance. In Module 4, you'll trace every step of this conversation end-to-end.

=== The observability challenge

The response looks great. But stop and think about what just happened behind the scenes:

* Which **agent** processed your question? Was it a single agent, or did multiple agents collaborate?
* Which **tools** did the agent invoke to gather pipeline data, denial rates, and performance metrics?
* How **long** did each step take? Was the LLM call fast, or did a tool call add latency?
* What if the response was **wrong**? How would you trace back to the root cause?
* What if a tool call **failed silently**? Would you even know?

Without proper observability instrumentation, these questions are unanswerable. The application gives you a polished response, but the entire decision-making process (the agent routing, tool invocations, LLM calls, and data retrieval) remains a black box.

**Why traditional monitoring isn't enough:**

Traditional application monitoring tracks HTTP requests, error rates, and response times. For the mortgage-ai API, that means you'd see:

* `POST /api/ceo/chat` → 200 OK, 2.3s response time

But you wouldn't see:

* Which tools the CEO agent called (`get_pipeline_summary`, `calculate_pull_through_rate`)
* How many LLM calls happened (was it 3 or 8?)
* Which call took 1.8s of that 2.3s total
* Whether the agent retrieved the correct data from the database
* If any tool calls failed silently but the agent still generated a response

This is why AgentOps requires specialized observability: you need to see inside the decision-making process, not just the HTTP wrapper around it. In the next modules, you'll learn how to instrument this visibility using the 3 pillars of observability.

[%collapsible]
.Under the hood: Production-ready AI patterns
====
This application demonstrates key patterns for regulated industries:

* **Multi-agent orchestration**: 5 LangGraph agents with role-scoped tools and RBAC enforcement
* **Compliance knowledge base**: RAG using pgvector with tiered boosting (federal regulations > agency guidelines > internal policies)
* **Model routing**: Complexity-based routing between fast and capable LLM tiers
* **Comprehensive audit trail**: Hash-chained, append-only audit events with MLflow trace correlation
* **PII masking**: Middleware-based masking for executive roles (SSN, DOB, account numbers)
* **Safety shields**: Input and output content filters with escalation pattern detection
====

== Module summary

**What you accomplished:**

* Explored the 5 agent personas and the multi-agent architecture
* Interacted with agents via the application UI
* Experienced the observability gap in distributed AI systems

**Key takeaways:**

* Multi-agent systems distribute decision-making across multiple components
* A single API can serve multiple agent personas with different capabilities
* Specialized observability approaches are required for AgentOps

**Next steps:**

Module 2 will introduce the 3 pillars of observability (metrics, logs, and traces) and how different personas (SRE/Platform Engineering vs. AI Developer/Engineer) approach monitoring these systems.
